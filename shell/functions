# vim:syntax=sh

function sublwhich() {
  subl $(which $*)
}

function codewhich() {
  code $(which $*)
}

function vimwhich() {
  vim $(which $*)
}

function nodeenv() {
  source "/usr/local/opt/nvm/nvm.sh"
}
