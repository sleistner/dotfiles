# vim:syntax=sh

alias l='ls --color -C -F'
alias ll='l -lh'
alias la='l -a'
alias l1='ls -1'
alias lla='ll -a'

# Ack
if [ ! -x `which ack` ];then
    alias ack='ack-grep'
fi

# Fast Ri
alias fri='fri -L'

