#!/usr/bin/env bash
# Decode a JWT: prints header + payload as pretty JSON.
# Usage: jwt-decode <token>

if ! command -v jq &>/dev/null; then
  echo "jq could not be found"
  exit
fi

jq -R 'split(".") | select(length > 0) | .[0],.[1] | @base64d | fromjson' <<<"$1"
