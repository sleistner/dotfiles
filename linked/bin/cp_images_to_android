#!/usr/bin/env ruby

from = ARGV.shift.sub(/\/$/, '')
to = ARGV.shift.sub(/\/$/, '')
name = ARGV.shift
as = ARGV.shift

%w(mdpi hdpi xhdpi xxhdpi).each do |resolution|
     cmd = "cp #{from}/drawable-#{resolution}/#{name}.png #{to}/drawable-#{resolution}/#{as}.png"
     puts cmd
     `#{cmd}`
end
